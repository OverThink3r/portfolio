import {courseItems} from "../../../data/courseItems";

export const Carousel = () => {
  return (
    <section className="max-w-[1400px] h-[780px] w-full m-auto py-16 px-4 relative">
      {/*<div style={{backgroundImage: `url(${courseItems[2].url})`}} className="w-full h-full rounded-2xl bg-center bg-cover duration-500">*/}

      {/*</div>*/}
    </section>
  );
};